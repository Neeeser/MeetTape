# Agent rules

Development notes, build and test commands and the invariants of this codebase
are in `CLAUDE.md`. Read that first.

- Design specifications and planning files under `.claude/specs/`, `plans/` and
  `probes/` are local investigation material. They are gitignored: never stage,
  commit or merge them.
